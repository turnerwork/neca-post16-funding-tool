/** Base URL for static assets (supports GitHub Pages project subpath). */
export const BASE_URL = import.meta.env.BASE_URL;

export const CSV_PATH = `${BASE_URL}Combined NECA data for allocations - Cursor.csv`;
export const LOGO_PATH = `${BASE_URL}North_East_MSA_Core_Lockup_black_RGB.jpg`;
